export const metadata = {
  title: "Admin Login | onroad",
  description: "Transport management system",
};

import LoginClient from './LoginClient';

export default function Page() {
  return <LoginClient />;
}